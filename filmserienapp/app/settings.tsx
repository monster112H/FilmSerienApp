import { Text, View } from "react-native";
import { StyleSheet } from "react-native";

export default function Main() {
    return (

        <View>
            <Text style={{backgroundColor: "green"}}>Einsteööungen</Text>
        </View>

    );
}